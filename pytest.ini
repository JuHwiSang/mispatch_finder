[pytest]
minversion = 8.0
addopts = -ra
testpaths = tests
pythonpath = src
markers =
    unit: unit tests
    integration: integration tests that may require network and tokens
filterwarnings =
    ignore::DeprecationWarning


