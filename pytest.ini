[pytest]
minversion = 8.0
addopts = -ra
testpaths = tests
pythonpath =
    src
    .
markers =
    unit: unit tests (core, shared)
    integration: integration tests that may require network and tokens (infra)
    e2e: end-to-end tests (app)
filterwarnings =
    ignore::DeprecationWarning


